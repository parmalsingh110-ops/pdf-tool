# PDF Media Suite — Python Backend Dependencies
# Security note: pin exact versions for reproducible builds.
# Run: pip install -r requirements.txt

# ── Web Framework ────────────────────────────────────────────────
# fastapi upgraded from 0.109.2 → security & Starlette updates
fastapi==0.115.6
# uvicorn upgraded from 0.27.1 → HTTP/2 + security fixes
uvicorn[standard]==0.32.1
gunicorn==23.0.0

# ── File Upload Security ─────────────────────────────────────────
# python-multipart upgraded from 0.0.9 → CVE fixes in form parsing
python-multipart==0.0.20

# ── PDF Processing ───────────────────────────────────────────────
pdf2docx==0.5.8
pdfplumber>=0.11.10
camelot-py[cv]>=2.0.0
# PyMuPDF upgraded from 1.23.21 → 1.25.5 security patches
PyMuPDF==1.25.5

# ── Office & Presentation ────────────────────────────────────────
openpyxl==3.1.2
python-pptx==0.6.23
python-docx==1.1.0

# ── Image Processing ─────────────────────────────────────────────
# Pillow: CVE-2024-28219 and others fixed in 11.x
Pillow>=11.1.0

# ── Data Science (required by camelot) ──────────────────────────
numpy

# ── OCR ──────────────────────────────────────────────────────────
ocrmypdf>=17.8.1
# iLovePDF OCR API — intentional existing feature, PRESERVED
ilovepdf>=1.0.0

# ── Environment & Rate Limiting ──────────────────────────────────
python-dotenv>=1.0.0
slowapi>=0.1.9
